import { Templates } from './template';

export class Transportation extends Templates {
  id: number;
  tabName?: string;
  anchorUrl?: string;
  isEnabled?: boolean;
  displayOrder?: number;
}
